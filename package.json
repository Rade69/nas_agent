{
  "name": "rileyjarvis",
  "version": "1.0.0",
  "description": "A local Electron desktop AI companion powered by OpenAI Realtime.",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "concurrently -k \"vite --host 127.0.0.1\" \"wait-on tcp:5173 && cross-env VITE_DEV_SERVER_URL=http://127.0.0.1:5173 electron .\"",
    "start": "electron .",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "check": "node --check electron/main.cjs && node --check electron/preload.cjs && node --check electron/services/pythonClient.cjs && node --check electron/core/env.cjs && node --check electron/core/ipc.cjs && node --check electron/core/legacyTools.cjs && node --check electron/core/window.cjs && node --check electron/core/companionWindow.cjs && node --check electron/core/secureWebPreferences.cjs && node --check electron/core/securitySelfTest.cjs",
    "smoke": "node scripts/smoke-test.cjs",
    "test": "cd python_backend && python -m pytest -q",
    "quality": "npm run typecheck && npm run check && npm run build && npm run test && npm run smoke",
    "package:dir": "npm run build && electron-builder --win --dir",
    "package:win": "npm run build && electron-builder --win",
    "ci": "npm ci",
    "audit": "npm audit --omit=dev"
  },
  "keywords": [
    "electron",
    "openai",
    "realtime",
    "voice",
    "desktop-assistant",
    "ai-companion"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^17.4.2",
    "lucide-react": "^1.22.0",
    "mermaid": "^11.16.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "concurrently": "^10.0.3",
    "cross-env": "^10.1.0",
    "electron": "^42.5.1",
    "electron-builder": "^26.15.3",
    "typescript": "^6.0.3",
    "vite": "^8.1.0",
    "vite-plugin-svgr": "^5.2.0",
    "wait-on": "^9.0.10"
  }
}
