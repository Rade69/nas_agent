# Ricky Branding Assets

Copy the contents of this folder into:

```txt
C:\Users\38765\Desktop\Nas-agent\assets\brending
```

Use:
- `logo/ricky-logo-r.svg` for small logo/header.
- `logo/ricky-app-icon.ico` for Windows app icon.
- `orb/ricky-orb-main.png` for main idle avatar.
- `orb/ricky-orb-mini.png` for minimized/floating companion.
- `icons/**` for UI icons.

Rules:
- Do not replace these assets with random icons.
- Do not use emoji as icon replacements.
- Use warning icons only for risk/confirmation.
- Use `currentColor` SVG icons with CSS classes.
